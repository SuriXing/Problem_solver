// Import the theme from our new location
import theme from './constants/theme';

// Define styles using JavaScript objects that we'll convert to CSS
export const homeStyles = {
  // Just define the theme so we can use it elsewhere
  // The actual styling will be done through the HomePage.css file
  theme
};

export default homeStyles; 